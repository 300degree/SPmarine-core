/** @format */

export enum TugboatType {
    SEA   = "SEA",
    RIVER = "RIVER",
};

export enum StatusType {
    SEA   = "SEA",
    RIVER = "RIVER",
};

export enum WaterStatusType {
    SEA   = "SEA",
    RIVER = "RIVER"
}

